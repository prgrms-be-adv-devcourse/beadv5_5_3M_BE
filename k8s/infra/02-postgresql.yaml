# PostgreSQL 18 + pgvector — 조원이 K3s worker 에 띄우는 매니페스트.
# - 이미지: pgvector/pgvector:pg18 (pgvector 익스텐션 포함)
# - init.sh (ConfigMap) 가 컨테이너 첫 기동 시 DB 7개 자동 생성:
#     ai_db, user_db, creator_db, payment_db, ticket_db, settlement_db, streaming_db
#     (movie-service 는 creator_db 공유 사용 — docker-compose 시절 그대로)
# - ai_db 에 pgvector 익스텐션 자동 활성화
# - 자격증명: postgres-secret 의 DB_USERNAME / DB_PASSWORD
# - StatefulSet + PVC 20Gi (local-path)
# - Service 이름: postgresql (값 변경 시 우리 values-*.yaml 의 DB_HOST 도 일치 필요)
# - master 에 dedicated=es:NoSchedule taint 있으므로 worker(t3.large) 로 자동 스케줄
apiVersion: v1
kind: ConfigMap
metadata:
  name: postgresql-init
  namespace: dev
data:
  init.sh: |
    #!/bin/bash
    set -e

    psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" <<-EOSQL
      CREATE DATABASE ai_db;
      CREATE DATABASE user_db;
      CREATE DATABASE creator_db;
      CREATE DATABASE payment_db;
      CREATE DATABASE ticket_db;
      CREATE DATABASE settlement_db;
      CREATE DATABASE streaming_db;
    EOSQL

    psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" --dbname "ai_db" <<-EOSQL
      CREATE EXTENSION IF NOT EXISTS vector;
    EOSQL
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgresql
  namespace: dev
spec:
  selector:
    matchLabels:
      app: postgresql
  template:
    metadata:
      labels:
        app: postgresql
    spec:
      containers:
        - name: postgresql
          image: pgvector/pgvector:pg17
          env:
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: DB_PASSWORD
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: DB_USERNAME
          resources:
            requests:
              memory: "512Mi"
            limits:
              memory: "768Mi"
          ports:
            - containerPort: 5432
          volumeMounts:
            - mountPath: /var/lib/postgresql/data
              name: postgresql-data
            - mountPath: /docker-entrypoint-initdb.d
              name: postgresql-init
      volumes:
        - name: postgresql-init
          configMap:
            name: postgresql-init
            defaultMode: 0755   # 실행 권한 부여
  volumeClaimTemplates:
    - metadata:
        name: postgresql-data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 20Gi
---
apiVersion: v1
kind: Service
metadata:
  name: postgresql
  namespace: dev
spec:
  selector:
    app: postgresql
  ports:
    - port: 5432
      targetPort: 5432
